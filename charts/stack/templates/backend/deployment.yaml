apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "stack.backend.fullname" . }}
  labels:
    {{- include "stack.backend.labels" . | nindent 4 }}
spec:
  {{- if not .Values.backend.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "stack.backend.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "stack.backend.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "stack.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}

      # =========================
      # Wait for Postgres to be ready
      # =========================
      initContainers:
        - name: wait-for-postgres
          image: busybox
          command:
            - sh
            - -c
            - |
              echo "Waiting for Postgres to be ready..."
              while ! nc -z {{ .Values.postgres.host }} {{ .Values.postgres.port }}; do
                echo "Postgres is not ready, sleeping 2s..."
                sleep 2
              done
              echo "Postgres is ready!"

      containers:
        - name: backend
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: {{ include "stack.backend.image" . }}
          imagePullPolicy: {{ .Values.backend.image.pullPolicy }}
          ports:
            - name: backend
              containerPort: {{ .Values.backend.service.port }}
              protocol: TCP
          resources:
            {{- toYaml .Values.backend.resources | nindent 12 }}
          env:
            - name: DATABASE_HOST
              value: "{{ .Values.postgres.host }}"
            - name: DATABASE_PORT
              value: "{{ .Values.postgres.port | quote }}"
            - name: DATABASE_NAME
              value: "{{ .Values.postgres.database }}"
            - name: DATABASE_USERNAME
              value: "{{ .Values.postgres.username }}"
            - name: DATABASE_PASSWORD
              value: "{{ .Values.postgres.password }}"
            - name: DATABASE_SSL
<<<<<<< HEAD
              value: "{{ .Values.postgres.ssl | default "false" }}"   # ensures "false" is passed as string
=======
              value: "{{ .Values.postgres.ssl | ternary "true" "false" }}"   # explicitly converts bool to "true"/"false"

>>>>>>> 15b66d8 (Fix backend SSL and wait for Postgres before startup)
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
